# prediction-mm — Polymarket US liquidity-reward maker.
# Runtime is deliberately tiny: poly_runner + polyclient use only the stdlib
# (urllib for HTTP, a hand-rolled .env parser) except for ED25519 signing.
cryptography>=41.0

# Tests use the stdlib `unittest` only — no extra test deps.
